.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# Swap the comments on the following lines if you wish to use zero-installs
# In that case, don't forget to run `yarn config set enableGlobalCache false`!
# Documentation here: https://yarnpkg.com/features/caching#zero-installs

#!.yarn/cache
.pnp.*

# Dependencies
node_modules/
package-lock.json
yarn.lock

# Environment variables
.env
.env.local
.env.*.local

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS files
.DS_Store
Thumbs.db

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# Test coverage
coverage/

# Build outputs
dist/
build/

# Temporary files
tmp/
temp/
*.tmp

# Session data (if we add file-based storage later)
sessions/
data/

# Vector store and knowledge base
# Note: vector_store/ is currently tracked in git for PoC convenience
# To untrack when size grows: git rm --cached -r vector_store/
# vector_store/  # Uncomment to stop tracking new changes
knowledge_base/uploads/  # User-uploaded files (PDFs, etc.)
